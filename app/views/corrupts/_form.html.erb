<%= form_for(@corrupt) do |f| %>
  <% if @corrupt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corrupt.errors.count, "error") %> prohibited this corrupt from being saved:</h2>

      <ul>
      <% @corrupt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit', class: 'button' %>
  </div>
<% end %>
